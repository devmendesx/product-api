server:
  port: 9000

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/productdb
    username: productdbu
    password: admin%
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true